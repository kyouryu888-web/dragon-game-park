# Dragon Game Park — Current Handoff

## 2026-08-24 追記: UNOカラー ルーレット演出の視認性を改善

作業ブランチ: `codex/uno-roulette-presentation`。

実装内容:

- ルーレット案内を中央モーダルと上部通知から分離し、テーブル直前の完全不透明な固定寸法パネルへ変更した。
- 見出し、主要メッセージ、プレイヤー名、枚数を別要素にして、340px以下では名前と枚数を別の行へ配置する。
- ルーレット中はパネルを消さず、山札から対象席へカード裏面を1枚ずつ飛ばす。指定色では1.2秒の
  セーフ表示へ切り替え、25枚到達時は既存の共通アウト演出を優先する。
- オンライン・オフラインとも1枚520msへ統一し、Realtimeとpollが同じ状態を返しても
  `sequenceKey` / `stepKey` が同じ演出を再生しないようにした。
- 読み上げは開始とセーフ／アウトだけにし、毎回の枚数更新は読み上げない。低モーション設定では
  カード飛来、回転、席の揺れを止め、文字と枚数更新だけを残す。
- 既存の最弱王ババ抜き160戦シミュレーションは実行環境によって5秒をわずかに超えたため、
  テスト内容を変えず当該テストだけタイムアウトを15秒へ安定化した。

検証:

- `npx tsc --noEmit`、製品build、17ファイル261テストが成功。lintはエラー0で既存警告のみ。
- 実ブラウザの320x844、390x844、1280x800で、長い名前、2桁枚数、不自然な改行、横はみ出し、
  テーブルとの重なりがないことを確認した。
- 実Supabaseのハード版6人ルームを2タブで操作し、人間の8枚ルーレットとCPUの2枚ルーレットが
  両画面で同一の演出ID・順番で各1回だけ表示され、versionも一致することを確認した。
- console error/warningは両画面0件。確認用ルーム4件は削除し、REST応答で残数0を確認した。

## 2026-08-24 追記: UNOドローカットインの表示時間を修正（未公開）

作業ブランチ: `codex/fix-uno-cinematic-duration`。

- 全面カットインのタイマーは1600msだったが、文字が不透明な区間は30%〜82%で、実際に読める時間が
  約832msしかなかった。さらに `prefers-reduced-motion: reduce` ではReact側もCSS側も300msで
  消していたため、端末設定によってはほぼ視認できなかった。
- 全面カットインを2400msへ変更し、文字の完全表示区間を18%〜90%（約1728ms）へ拡大した。
  導入・退出を含めても画像と文言を落ち着いて確認できる。
- Reactの消去タイマーとCSSアニメーションを `getUnoCinematicDuration` 由来のCSS変数で接続し、
  時間設定が片方だけずれないようにした。
- 低モーション設定では移動・飛来を止める一方、表示時間は2400msのまま維持する。
- 実ブラウザで強制ドローカットインを発生させ、DOM表示2394ms、CSS `2.4s` を確認した。
  続けて実際のドロー返しも発生させ、DOM表示2263ms、文字の不透明度95%以上の可読時間1640ms、
  console error/warning 0件を確認した。
- `npx tsc --noEmit`、製品build、17ファイル258テストが成功。lintはエラー0で既存警告のみ。

## 2026-08-24 追記: 4ゲームのオンライン参加導線を短縮

実装内容:

- UNO、マンカラ、バックギャモン、最弱王ババ抜きの対戦方法を、初回表示では
  `遠くの人とオンライン` に統一した。オンライン内の初期タブも `コードで参加` に統一した。
- UNO、マンカラ、最弱王ババ抜きは、共通設定画面で正しい6桁コードを入力して進むと、
  従来のルーム画面にあった2回目の `参加する` 操作を自動実行する。
- 自動参加中は通信が長い場合だけ待機表示を出す。満員、無効コード、通信失敗などの場合は、
  入力済みコードとエラーを保持した従来の参加画面へ戻るため、手動で再試行できる。
- バックギャモンは以前から設定画面から直接参加する構成だったため、初期選択だけを共通化した。
- `gameSetupDefaults.ts` に共通初期値と自動参加条件を集約し、契約テストを追加した。

検証:

- `npx tsc --noEmit`: 成功
- `npm run build`: 成功（英語音声100件検証成功、既存の500kB超chunk警告のみ）
- `npm test -- --run`: 17ファイル / 257テスト成功
- `npm run lint`: エラー0。既存のBackgammon/Mancala警告のみ
- 実ブラウザ390x844で4ゲームすべてがオンライン／コード参加を初期表示することを確認した。
- 実SupabaseのUNOルーム `4924EB` を使い、共通設定画面へコードを入力すると2回目の参加ボタンを
  表示せずホスト・ゲスト両方が対戦画面へ進むこと、console error/warningが0件であることを確認した。
  確認用ルームは削除し、REST応答で残数0を確認した。

## 2026-08-24 追記: UNOドロー演出を追加

作業ブランチ: `codex/uno-standard-draw-stacking`。通常版のドロー重ねコミット `8ee2cc3` の上に
演出コミット `2bed30b` を追加し、ブランチをpush済み。上記のオンライン導線改善と同じPRで公開する。

実装内容:

- ユーザー選択のB案（赤いドラゴンがカードを跳ね返す漫画カット）を基準に、縦3:4の
  `draw-counter.webp` / `forced-draw.webp` / `knockout.webp` を960x1280で追加した。
- ドローカードを実際に重ねた時だけ「ドロー返し」、返さず累積枚数を引いた時は
  「ドロー受け取り」、引いた結果25枚へ達した時は受け取りを省略して共通の「アウト」を表示する。
- `ワイルド リバース ドロー4` は「むきを変えて返した！」、通常の返しはカード名と累積枚数を
  日本語で画像上へ重ねる。1.6秒の間は人間操作とCPU思考を止め、状態更新とオンライン同期は先に行う。
- カラー ルーレットは `drawnCount` を保留状態へ保存し、1枚ごとの色の輪・カード飛来・現在枚数と、
  指定色が出た時のセーフ表示を追加した。ルーレットで25枚へ達した時も共通アウト演出を使う。
- Realtimeと5秒pollで同じ状態を再受信しても、`gameId`・ターン・場札・内容から作るイベントキーで
  同じ演出を二重再生しない。オフライン、CPU入り、オンラインで共通フックを使う。
- `prefers-reduced-motion` では飛来・ズームを止め、300msの短いフェードにする。
- 演出専用テスト8件を追加し、ルーレットの保存枚数を既存ルールテストにも追加した。

検証:

- `npm test -- --run`: 17ファイル / 256テスト成功
- `npm run build`: 成功（英語音声100件検証成功、既存の500kB超chunk警告のみ）
- `npm run lint`: エラー0。既存のBackgammon/Mancala警告のみ
- 実ブラウザ390x844で3画像とルーレット表示、1280x800でドロー返しを確認。最長テスト名と
  2桁の累積枚数も枠内に収まり、console error/warningは0件。
- 実際のハード版4人戦で、プレイヤーのワイルド ドロー4からCPUが連続で返し、累積24枚を
  プレイヤーが受け取って25枚アウトになるまで操作。返し演出とアウト演出が正しい順序で1回ずつ表示された。
- 実Supabaseの通常版2人ルームを2タブで開始し、ゲストがドロー2、ホストが累積2枚を受け取る流れを操作。
  両タブに同じ受け取り演出が1回表示され、versionも一致、console error/warningは0件だった。
  確認用ルーム `2C3SQ4` は削除し、REST応答で残数0を確認した。

## 2026-08-23 追記: UNOのドロー重ねを通常版へ追加

作業ブランチ: `codex/uno-standard-draw-stacking`。コミット `8ee2cc3` をpush済み。

実装内容:

- 通常版でも `ドロー2` と `ワイルド ドロー4` を重ねられるようにした。直前のドローカードと
  同じ枚数か、それ以上の枚数だけを出せる（+2には+2/+4、+4には+4）。
- ハード版の `ワイルド リバース ドロー4` をドロー4として重ねられるようにした。
  色を選んだ後に進行方向を反転し、反転後の次プレイヤーへ累積ドローを渡す。
- 通常版のオンライン状態を読み直した際に、正しい累積ドローが0へ消されないよう状態補正を更新。
- CPUも共通の出せるカード判定を使うため、重ねられるカードがあれば出し、なければ累積枚数を受け取る。
- 通常版ルールパネルとカード効果説明へ、ドロー重ねの説明を追加。

検証:

- `npx tsc --noEmit`: 成功
- `npm run build`: 成功（英語音声100件検証成功、既存の500kB超chunk警告のみ）
- `npx vitest run`: 16ファイル / 248テスト成功
- `npm run lint`: エラー0。既存のBackgammon/Mancala警告のみ
- ローカル実ブラウザで通常版ルールの重ね出し説明を確認。console error/warningは0件。

## 2026-08-22 追記: UNOのドロー効果と脱落後の手番を修正（PR #16）

実装PR: [#16](https://github.com/kyouryu888-web/dragon-game-park/pull/16)。
作業ブランチ: `codex/fix-uno-action-effects`。

修正内容:

- 通常版で最後の1枚として `ドロー2` または `ワイルド ドロー4` を出すと、勝利判定が先に走り、
  次のプレイヤーがカードを引かない不具合を修正。ペナルティーを解決してから得点と勝敗を確定する。
- ハード版でドローにより25枚へ達した際、脱落処理と呼び出し元の双方が手番を進めて次の生存者を
  飛ばす不具合、および他人に存在しない「引いたカードを出す」操作待ちを付ける不具合を修正。
- カラールーレット中に引けるカードがなくなった場合の停止、存在しない／脱落済みプレイヤーとの
  7交換、通常版への状態整形時の山札・場札ID重複と無効な保留操作を修正。
- `unoSimulation.test.ts` を追加。通常版4人×3、ハード版4人×3、通常版10人、ハード版6人を
  CPUだけで決着まで進め、毎手で総カード枚数、ID重複、モード外カード、脱落者の手番、
  引いたカード操作待ちの整合性を検査する。

検証:

- `npx tsc --noEmit`: 成功
- `npm run build`: 成功（英語音声100件検証成功、既存の500kB超chunk警告のみ）
- `npx vitest run`: 16ファイル / 242テスト成功
- `npm run lint`: エラー0。既存のBackgammon/Mancala警告のみ
- 実ブラウザ390x844と1280x800で通常版を開始し、山札クリックで山札93→92枚、手札7→8枚を確認。
  横スクロール、Vite error overlay、console errorはいずれも0。
- Supabaseの実ルーム作成まで疎通確認。自動検証で作成したルームはすべて削除済み。

## 2026-08-15 追記: ババ抜き改善と4ゲーム共通UIを本番反映

実装PR: [#14](https://github.com/kyouryu888-web/dragon-game-park/pull/14)。
マージコミット: `5ed9b32e85240c626052b195216037e96be65dfd`。
PR先端とマージコミットの両方で GitHub Actions `build-and-test` と Vercel が成功し、
`https://dragon-game-park.vercel.app/` へ本番反映済み。

実装内容:

- `GameSetupFlow.tsx` を追加し、マンカラ・UNO・バックギャモン・最弱王ババ抜きの設定画面を
  共通の3段階（I. 名を刻む / II. 対戦方法を選ぶ / III. 対戦相手・ルームを決める）へ統一した。
- 名前欄は `挑戦者の名（なくてもよい）`、オンラインはユーザー指定どおり
  `遠方の者と対戦 ONLIE`、`ルームを作成` / `コードで参加`、参加欄は `コードを入力` に統一した。
- 1台で交互に操作できるマンカラとバックギャモンだけに `同じ盤で対戦` を表示。
  UNOとババ抜きはカード秘匿が必要なため、CPU対戦とオンライン対戦だけを表示する。
- トップからUNOを選んだ直後の戻る操作が旧UNO設定画面へ入る配線を修正し、直接ホームへ戻すようにした。
- ホームへ戻るボタン名を `ゲーム選択に戻る`、設定へ戻るボタン名を `ゲーム設定に戻る` に統一した。
- `GameEndActions.tsx` を追加し、4ゲームのローカル・オンライン終了画面へ
  `再戦する` / `設定を変更して再戦する` / `ゲーム設定に戻る` / `ゲーム選択に戻る` を接続した。
- PCでは設定画面を最大1180pxの2カラム、スマホでは1カラムに切り替える。プレイ画面もPC用に拡張し、
  マンカラは2人盤900px・3/4人盤620px、バックギャモン920px、ババ抜き盤720px、UNOは
  1120pxのアプリ幅を使う。スマホでは既存のタッチ向け寸法を維持する。

検証:

- `npx tsc --noEmit`: 成功
- `npm test -- --run`: 15ファイル / 219テスト成功
- `npm run build`: 成功（英語音声100件検証成功、既存の500kB超chunk警告のみ）
- `npm run lint`: エラー0。既存のBackgammon/Mancala警告のみ
- `git diff --check`: 成功（WindowsのLF→CRLF予告のみ）
- 実ブラウザで幅1440x900と390x844を切り替え、4ゲームの設定画面とローカルプレイ画面を確認。
  設定画面はPCで1180pxの2カラム、スマホで351pxの1カラム。全画面で横スクロールなし。
- UNOのホーム→設定→`ゲーム選択に戻る`がホームへ戻ることを実操作で確認。
  ブラウザconsole error/warningは0件。

補足:

- 見た目の基準としてダークファンタジー＋金/熾火のPC/スマホ設定画面案を生成し、3段階構成、
  配色、パネル形状を反映した。案にあった設定画面内のゲーム切替サイドバーは、ホームのゲーム選択と
  二重になるため採用していない。
- 本番でバックギャモン設定画面のPC 2カラム表示、オンライン作成/参加タブ、`コードを入力`、
  UNOのスマホ1カラム表示、カードゲームに `同じ盤で対戦` が出ないこと、UNOから
  `ゲーム選択に戻る` で直接TOPへ戻ることを確認。横スクロールとconsole error/warningは0件。

## 2026-08-15 追記: 最弱王ババ抜きのシャッフル演出を強化（未公開）

作業ブランチ: `codex/babanuki-followup-fixes`。前回修正コミットの上に追加実装中。まだpush・PR・本番公開はしていない。

追加内容:

- シャッフルタイムをゲームの見せ場として、宣言・サイコロ判定・手札移動の2段階演出へ刷新した。
  固定パネルに出目、回転方向、実際の移動先、ブラフ解除／維持を表示し、移動中も消さない。
- 手札移動は従来の小さな3枚束から、光る大きな4枚束へ変更。盤面中央に回転エフェクトを出し、
  出目3は中央へ集合後に再配布、出目4はカードを消さずドクロだけを表示する。
- 表示時間を、サイコロ判定 `1100ms → 1700ms`、通常移動 `900ms → 1250ms` に延長し、
  到着後に結果を読める `500ms` の停止時間を追加。出目3は2段移動＋停止で合計3000ms。
- ローカル対戦とオンライン対戦で同じ演出状態を使う。説明文は `describeDice` で一元化し、
  全出目の方向・効果・ブラフ扱いをテストした。
- PC幅1280×720とスマホ幅390×844の実ブラウザで、サイコロ判定と手札移動の両段階を確認。
  固定パネルは画面内に収まり、移動中も内容が読めた。console error/warningは0件。

検証:

- `npx tsc --noEmit`: 成功
- `npm run build`: 成功（英語音声100件検証成功、既存の500kB超chunk警告のみ）
- `npx vitest run`: 14ファイル / 217テスト成功
- `npm run lint`: エラー0。今回変更したファイルの警告は0、既存ファイルの警告のみ
- 実ブラウザのPC幅1280×720とスマホ幅390×844で、サイコロ判定と手札移動の両段階を撮影確認。
  出目・方向・移動内容・ブラフ解除が移動中も読め、カード束が中央を経由して動く。console error/warningは0件。

## 2026-08-15 追記: 最弱王ババ抜きの操作不具合と同設定再戦を修正（未公開）

作業ブランチ: `codex/babanuki-followup-fixes`。ローカルコミット済み。まだpush・PR・本番公開はしていない。

修正内容:

- シャッフルボタンは、共通 `.btn:active` が絶対配置の `translate(-50%,-50%)` を上書きし、
  押下中に当たり判定が指の下から動いていた。専用クラスで中央配置を保つ押下変形へ変更し、
  `z-index` と `aria-label` も追加した。
- ブラフ中だけ「ブラフを解除」ボタンを表示し、ダブルタップが難しい重なった手札でも
  確実に解除できるようにした。
- 仕様を更新し、シャッフルタイムで手札が動く出目（1・2・3・5・6）は全員のブラフを解除、
  何も動かない出目4だけ維持する。ルール状態と演出用表示状態の両方を同じ挙動にした。
- `createBabanukiRematchState` を追加。人数・名前・人/CPU・CPU強さを保持し、権利・順位・手札を
  初期化してイベント連番を単調増加させる。これにより前局の盤面が一瞬残らず再戦へ移れる。
- オンライン決着後、ホストに「同じ設定で再戦」「設定を変える」を表示。同じルーム行の
  `game_state` を更新するので、購読中の参加者はそのまま次局へ移る。ゲストには待機案内を表示する。
- 人間のシャッフル宣言者名が、名前未入力時にCPU名で表示されていた既存の表示ミスも修正した。

検証:

- `npx tsc --noEmit`: 成功
- `npm run build`: 成功（英語音声100件検証成功、既存の500kB超chunk警告のみ）
- `npx vitest run`: 13ファイル / 208テスト成功
- `npm run lint`: エラー0。既存ファイルの警告のみ
- 実ブラウザ（`http://127.0.0.1:5175/`）でローカル3人対局を決着まで進め、
  シャッフルボタンの実押下、ブラフ設定→明示解除、同じ設定での再戦を確認
- 実Supabaseのオンライン3人（ホスト＋CPU2体）を決着まで進め、「同じ設定で再戦」後に
  終了画面が消え、同じ3席で新しい手札・順位・シャッフル権へ即時リセットされることを確認
- 実Supabaseの2タブ3人（ホスト＋ゲスト＋CPU）でも決着まで進め、ホストには再戦ボタン、
  ゲストには待機案内が出ることを確認。ホストが再戦すると両タブが同じ新局へ自動移行した
- 両タブを含めブラウザconsole error/warningは0件。確認用ルーム `JPF5FK` / `4BXVJJ` は
  どちらも削除し、REST応答 `[]` を確認

## 2026-08-15 追記: 最弱王ババ抜きを本番公開

- 実装PR: [#12](https://github.com/kyouryu888-web/dragon-game-park/pull/12)
- 機能のマージコミット: `55c227996eaa688a4ec0bae8dd2b8a22e4e82f5b`
- PR先端とマージコミットの両方で GitHub Actions `build-and-test` と Vercel が成功
- 本番URL: `https://dragon-game-park.vercel.app/`
- 本番でトップ画面の「最弱王ババ抜き」表示、オンラインルーム作成、6桁コード表示、
  待機画面、ルームを閉じて削除できることを確認。ブラウザconsole error/warningは0件
- 本番確認用ルームは削除し、Supabase REST応答が空配列 `[]` であることを確認

## 2026-08-15 追記: 最弱王ババ抜きのオンライン疎通確認完了

ユーザーが `supabase/babanuki_rooms.sql` を Supabase SQL Editor で実行し、
`Success. No rows returned` を確認した。

初回の2タブ確認で、ホストとゲストが同じオンラインIDになり、ゲストがホストとして
再入室扱いになる不具合を再現した。参加者IDを全タブ共通の `localStorage` に保存していたのが原因。
`babanukiOnline.ts` をタブ単位の `sessionStorage`（`dgp-babanuki-online-player-id`）へ変更し、
同じタブではIDを維持し、別タブでは別IDになる回帰テストを追加した。
PR #12 の初回CIでは、このテストがSupabase環境変数の無いCI上で通信クライアントを生成して
失敗したため、テスト内でSupabaseクライアントをモックし、外部環境に依存しない形へ修正した。

修正後の実ブラウザ確認（ホスト＋ゲスト＋CPUの3人ルーム）:

- 6桁コードで作成・別タブ参加し、2人の人間席が埋まると自動開始
- ルーム作成時に席ごとの人／CPU切り替えとCPU強さ選択を確認
- ホストとゲストが別プレイヤーとして、自分の手札と手番を正しく表示
- 2段階の札引き（選択→「この札を引く」）と、引く／ペア／シャッフルタイムの同期を確認
- 人間6手、CPU4手を経て停止や二重進行なしで決着。CPU処理はホスト側だけで進行
- 両画面で同じ最弱王・順位を表示し、「ルーム設定へ」で作成／参加画面へ戻れることを確認
- 両タブのブラウザconsole error/warningは0件
- 確認用に作成したSupabaseルーム行は終了後に削除済み

最終ローカル検証:

- `npx tsc --noEmit`: 成功
- `npm run build`: 成功（英語音声100件検証成功、既存の500kB超chunk警告のみ）
- `npx vitest run`: 12ファイル / 204テスト成功

## 2026-08-15 追記: ババ抜きのデバッグ（進行停止バグを2件修正）

**バグ1（致命的・修正済み）: CPUの手番中に手札を触り続けると進行が永久に止まる。**
CPUの「3秒考える」タイマーを張る `useEffect` の依存配列に `logic`（状態オブジェクト全体）を
入れていたため、並べ替えやブラフのたびに新しい状態が作られてタイマーがリセットされ、
CPUが永遠に引かなくなっていた。90msごとに自分の手札をタップし続ける乱打テストで再現し、
**局面が本当に変わったときだけ張り直す `turnKey`（`currentPlayerId:phase:eventSeq`）**へ変更して解消。
最新状態は `logicRef` から読む。`BabanukiPlayScreen.tsx` と `BabanukiOnlineGame.tsx` の両方。

**バグ2（オンライン・予防的修正）: 書き込み中の操作が捨てられて手が消える。**
`applyAction` が `writingRef` で「書き込み中なら無視」していたため、並べ替えの通信中に
CPUの手番が来るとその手が消えて進行が止まりうる。**書き込みをPromiseで直列につなぐ**方式に変更。
updater は実行時点の最新状態に対して走るので、順番待ちでも結果は正しい。

**追加した回帰テスト**: `babanukiSimulation.test.ts`。3〜6人×40シード＝160局をCPUの判断で
最後まで自動対局させ、毎ステップで不変条件（札は常に53枚・重複なし・ジョーカーは捨て札に
出ない・順位は連番・宣言できるのはジョーカー保持者だけ）を検査する。
シャッフル権が1人1回までであること、ブラフが引かれるまで外れないことも別途検査。

## 2026-08-14 追記: 4つ目のゲーム「最弱王ババ抜き」を追加（オンラインは未検証）

仕様は **`docs/babanuki-spec.md` が唯一の正**。新ゲーム追加の手順は `docs/NEW_GAME_CHECKLIST.md`。

実装したもの（`src/features/babanuki/`）:

- `babanukiRules.ts` / `babanukiCpu.ts` … 純ルールとCPU。テスト38件（`npm test` 全体で192件パス）
- `useBabanukiPlayback.ts` … `BabanukiState.events` を1つずつ再生する共通フック。
  ローカルもオンラインも同じ演出になる
- `BabanukiTable.tsx` … 座席・手札の描画と、カードの飛行アニメ
  （スロットの矩形を控えて `position: fixed` を double-RAF ＋ CSS transition で移動）
- `BabanukiFinale.tsx` … 最弱王の戴冠式（暗転→スポットライト→ジョーカー→💀の王冠→称号→順位表）
- `babanukiOnline.ts` ほか … ルームコード方式。`version` 付き update の楽観ロックで
  シャッフルタイムの同時宣言を裁定。CPUと締め切り判定はホストのみ実行
- keyframes は `src/styles/global.css` の末尾に `babanuki-` 接頭辞で追加
- 配線4箇所（`games.ts` / `HomePage.tsx`の3マップ / `App.tsx` / featureフォルダ）は完了

検証済み: ローカル対戦を3人・6人で通しプレイ（375px幅・横スクロールなし）、
引く／ペア捨て／シャッフルタイム（宣言→サイコロ→手札移動）／勝ち抜け／戴冠式の演出、
`npx tsc -b` と `npm run build` と `npx vitest run`。

**未検証: オンライン対戦。** `supabase/babanuki_rooms.sql` をユーザーがSupabaseで実行するまで
テーブルが無いため、ルーム作成が「ルームを開けなかった」で失敗する（クラッシュはしない）。
SQL実行後に2タブで疎通確認すること。

## 2026-08-14 追記: UNOスタートプレイヤー決定パネルの進行不能修正

- 人数や同点再抽選が増えると抽選結果一覧が中央テーブルの外へ伸び、「ゲーム開始」ボタンが見えず押せなくなる不具合を修正した。
- 中央オーバーレイをテーブル内の上下余白つき領域へ変更し、抽選結果一覧だけを縦スクロール可能にした。
- スタートプレイヤー表示と「ゲーム開始」ボタンはスクロール領域の外へ置き、常に表示されるようにした。
- プレイヤー名・引いたカード・点数の行をコンパクトにし、スマホでも内容を判別できるようにした。
- 390x844、通常版10人（人間1人+CPU9人）、同点再抽選を含む14結果行で確認した。結果欄は `clientHeight: 116 / scrollHeight: 1244` で内部スクロールし、「ゲーム開始」は画面内に表示・有効だった。
- 「ゲーム開始」を実際に押し、抽選パネルが消えて選出プレイヤーのターンが開始すること、ブラウザのconsole error/warningが0件であることを確認した。

## 2026-08-14 追記: UNOルーム再入室・スターター演出・通常版カード混入対策

今回の作業:

- 通常版UNOにハード専用カードが紛れた状態を検出・除外する `sanitizeUnoStateForVariant` と、通常版ではハード専用カードを出せないガードを追加した。
- ゲーム開始前のスタートプレイヤー決定を2段階化した。まず全員が1枚引いた結果をカード画像と点数で表示し、その後「ゲーム開始」ボタンを押してから `playing` に入る。
- 手札表示を赤→黄→緑→青→ワイルドの順にソートし、多枚数時の重なりを浅くして数字/記号ラベルを読みやすくした。
- UNOオンライン待機画面に「コードをコピー」ボタンを追加し、参加コード入力欄の placeholder を「ここにコードを入力」に変更した。
- オンライン再戦でCPU枠を人間に変えたり人間を追加した場合、ルームコード待機画面を表示し、相手が参加するまで待つ流れに変更した。
- ルームが満員扱いでも、同じ名前の元参加者が再入室する場合は既存の人間スロットへ復帰できるようにした。

検証済み:

- `npx tsc --noEmit`: 成功
- `npx vitest run src/features/uno/unoRules.test.ts src/features/uno/unoOnline.test.ts`: 2 files / 33 tests passed
- `npx vitest run`: 8 files / 154 tests passed
- `npm run build`: 成功。Viteの500kB超chunk警告のみ
- `npm run lint`: 成功。既存のMancala/Backgammon警告のみ
- `git diff --check`: 成功。Windows改行予告のみ

ブラウザ確認済み:

- ローカルの `http://127.0.0.1:5175/` を Chrome + Playwright で確認した。
- 通常版UNOのオンラインルームを2タブで作成・参加し、両方がスタートプレイヤー決定画面へ進むことを確認した。
- 各プレイヤーが引いたカードと点数が表示され、ホストが「ゲーム開始」を押した後に両タブが対戦画面へ進むことを確認した。
- 3人ルームの待機画面にルームコードと「コードをコピー」が表示されることを確認した。
- 満員の2人ルームに、別ブラウザ状態から同じ名前で再入室でき、「満員です」で弾かれないことを確認した。
- 上記確認中にブラウザのコンソールエラーは発生しなかった。

残る注意点:

- 同名再入室は表示名を照合するカジュアルルーム向けの復旧方法。同じ名前の参加者が複数いる場合や、再入室時に名前を変更した場合は判別できないため、将来は再入室用トークンを導入するとより堅牢になる。

## 2026-08-13 追記: UNO改善を本番反映済み

- ブランチ `codex/uno-start-draw-polish` の変更を `main` に `--no-ff` でマージコミットした。
- マージコミット: `ccc728e404e9a0c704607e64580d2eb44548fc0c`
- `git push origin main` 済み。Vercelのコミットステータスは `success`。
- 本番URL `https://dragon-game-park.vercel.app/` は HTTP 200 を確認済み。
- マージ後の `main` で以下を再実行済み:
  - `npx tsc --noEmit`: 成功
  - `npx vitest run`: 8 files / 150 tests passed
  - `npm run build`: 成功。Viteの500kB超chunk警告のみ
  - `npm run lint`: 成功。既存のMancala/Backgammon警告のみ

## 2026-08-13 追記: UNO開始判定・1枚ドロー・再戦設定UI

今回の作業:

- UNOの初期状態を `deciding-starter` に変更し、ゲーム開始前に「カードを引いて決める」パネルを表示するようにした。
- `applyStarterDraw` を追加。各プレイヤーが山札から1枚引き、数字カードは数字、記号/ワイルドは0として最大値の人をスタートプレイヤーにする。同点は同点者だけで再ドローする。
- 通常ドローを「1枚だけ引く」仕様へ変更。手札に出せるカードがあっても引けるが、引いた後に出せるのは `drawn-card-play` で保持した引いたカードだけ。出さない場合は「出さずに次へ」でターンが進む。
- ハード版の通常ドロー説明から古い「むげんドロー」を削除。カラールーレットは従来どおり専用効果として複数ドローする。
- 中央パネルとプレイヤー情報窓を不透明寄りにして、暗い背景やカードの上でも読めるようにした。
- 手札が多い時の重なり間隔を広げ、dense表示ではカード上に小さな数字/記号ラベルを出すようにした。
- オンラインUNOの終了画面に、同じルームで通常/ハード、人数、人間/CPU、CPU強さ、名前を設定して再戦できるホスト用フォームを追加した。

検証済み:

- `npx tsc --noEmit`: 成功
- `npx vitest run`: 8 files / 150 tests passed
- `npm run build`: 成功。Viteの500kB超chunk警告のみ
- `npm run lint`: 成功。既存のMancala/Backgammon警告のみ
- in-app browser `http://127.0.0.1:5175/`
  - UNOオフライン開始時に「スタートプレイヤー決定」パネルが出ることを確認
  - 「カードを引いて決める」後にゲームが始まり、山札ボタンが押せることを確認
  - 出せるカードがある状態でも山札を引けて、引いたカードだけが出せる保留になることを確認
  - 390x844相当で中央パネルが読め、横はみ出しがないことを確認

次に見るとよいこと:

1. Supabaseを使った実際のオンライン2タブで、終了後の再戦設定フォームから通常/ハード・人数・CPU設定を変えて開始できるか確認する。
2. 20枚以上の手札で dense ラベルが十分読めるか、実プレイまたはテスト用状態で追加確認する。
3. 本番反映後、`https://dragon-game-park.vercel.app` でUNO画面の初回表示とオンラインルーム作成を軽く確認する。

## 2026-08-12 追記: UNOオンライン再戦選択・先手公平化・手札UI改善

今回の作業:

- `src/features/uno/createInitialUnoState.ts`
  - UNO開始時に必ず `player-1` から始まる状態をやめた。
  - 配布後、各プレイヤーが数字カードを1枚めくった想定で、最大数字を出したプレイヤーを `currentPlayerId` にする。
  - 同点の場合は同点者からランダム。判定に使った数字カードはシャッフルして山札へ戻す。
- `src/features/uno/unoRules.ts`
  - 出せるカードがない時の山札ドローを、通常版でも「出せるカードが出るまで引き、その場で出す」に変更。
  - ハード版の既存挙動に通常版を合わせた形。
- `src/features/uno/unoRules.test.ts`
  - 通常版で、出せないカードを引いた後、出せるカードを引いたら即場に出るテストを追加。
- `src/features/uno/UnoOnlineGamePage.tsx`
  - オンラインUNO終了後、ルーム主に「通常版で再戦」「ハード版で再戦」を表示。
  - 同じ `uno_rooms` の `game_state` を初期化してルームを作り直さず続行する。
  - ハード版は6人までなので、7人以上のルームではハード版再戦ボタンを無効化。
- `src/features/uno/UnoTableView.tsx`
  - 手札が15枚以上の時は扇形を抑え、横スクロール前提で重なりを浅くする `is-dense` 表示へ切替。
  - 多枚数でもカード内容と「出せる」表示が隠れにくい方向へ調整。
- `src/styles/global.css`
  - UNO中央選択パネルを不透明寄りにし、暗幕・枠線・文字コントラストを強化。
  - オンラインUNO再戦パネルと、通常版/ハード版の選択ボタンを追加。
  - 多枚数手札用の横スクロールと `is-dense` スタイルを追加。

検証済み:

- `npx tsc --noEmit`: 成功
- `npx vitest run src/features/uno/unoRules.test.ts src/features/uno/unoOnline.test.ts`: 2 files / 25 tests passed
- `npm test -- --run`: 8 files / 146 tests passed
- `npm run build`: 成功。既存の500kB超chunk警告のみ。
- `npm run lint`: 終了コード0。既存のMancala/Backgammon警告のみ。
- `git diff --check`: 成功。Windows改行予告のみ。

未確認:

- この実行環境ではバックグラウンドのVite起動がポリシーで止まり、ブラウザ実画面確認まではできていない。
- 次に見るべき実画面:
  1. UNOで色選択/7交換/ルーレットの中央パネルが暗い背景でも読めるか。
  2. 手札15枚以上、24枚付近で横スクロールしながらカードが読めるか。
  3. オンラインUNO終了後、ホストに通常版/ハード版再戦ボタンが出るか。
  4. 再戦後、`player-1` 固定ではなく別プレイヤーが先手になることがあるか。
  5. 通常版でも、出せない時に山札から出せるカードまで引き、そのカードが即場に出るか。

更新日: 2026-08-12
ブランチ: `main`
実装PR: [#8](https://github.com/kyouryu888-web/dragon-game-park/pull/8)（2026-08-05 マージ済み）
非公開化PR: [#9](https://github.com/kyouryu888-web/dragon-game-park/pull/9)（2026-08-12 マージ済み）

## 公開状態

- ユーザーから「どれも音声を聞いて選択するだけでワンパターン」と評価され、作り直すまで英語学習ゲームを非公開にした。
- PR #9のマージコミット`bc77116`で、トップ一覧、App画面遷移、遅延ロード配線を削除した。英語学習ゲームのソースは再設計用に保持している。
- `npx tsc --noEmit`、`npm run build`、`npx vitest run`（8ファイル145件）、デスクトップと390px相当の画面確認に合格した。
- PR #9とマージコミットで`build-and-test`、Vercelが成功している。

## 現在の目的

「イングリッシュ ラーニング オデッセイ」は非公開。再公開前に、音声選択の反復ではなく、遊びそのもののルールと英語使用が一体化したゲームへ再設計する。

## Gameplay v4までに実装したこと

- 英字を読ませない絵と音の初回案内を3問から6問へ拡張。診断成績で物語を飛ばさず、経験者には難しめの項目を混ぜる。
- 12話すべてに固有の物語、案内役、目的、報酬、教材を設定。100教材を重複なく12話へ割り当てた。
- 各話の前に絵本型の`QuestBriefing`を表示し、「次に何をするか」を一つのボタンで案内する。
- 精霊8種を第1〜8話で順に獲得。獲得前の進化を禁止し、別日3回・2モード以上の成功で進化可能にした。
- 4ゲームを話ごとの教材で動くように変更。
  - 精霊捕獲: 探索→発見→音と絵のお供え。
  - ドラゴンアリーナ: 移動・回避に加え、運動操作が苦手な子向けのタップ作戦。
  - ことば錬金: 音・絵・文字のマッチングと、後半の能動的な語順パズル。
  - 記憶の脱出: 看板と音声を集め、鍵へ結合して出口を推理。
- 最終章を通常ゲームの再利用から、4部屋の`FinalDungeon`へ変更（音の門、色の橋、語順の炉、予定の蔵）。失敗しても進行を失わない。
- 物語セッションを復習50%・新出35%・混合15%へ寄せる`composeQuestSession`を追加。
- 通常4ゲームの誤答は即時反復せず、決定的な共通キューで3〜5問後に一度だけ再出題する。再出題では正解対象を発光させ、ヒント正解として記録する。短い末尾は橋渡し問題で間隔を確保し、再誤答で無限連鎖しない。
- 保護者画面にJSONファイルの書き出し・復元に加え、貼り付け復元を追加。
- 13章トラックと次の行動を地図に表示。デスクトップの黒帯をなくし、390×844の縦見切れを解消。
- 最終ダンジョンの背景は生成済みWebP、HUDと操作部はReact/CSSで実装。設計原画は`docs/design/english-quest-*-v3.png`。
- Kokoro-82M `0.9.4` / `af_heart`で第1島100件の米国英語MP3を生成。24kHz mono・64kbps・音量正規化済みで、モデルはWebアプリへ含めない。マニフェストに話者、モデル、長さ、音量、SHA-256を保存する。
- 会話8件は孤立した質問から、前置きと応答を含む2往復型へ変更。音声生成は既存ハッシュを検証して変更分だけ作り直せる。
- 保護者メニューに100件を順番に再生できる音声見本パネルを追加。各音声を「聞き取りOK／要再確認」に分類し、確認数を端末内保存・JSON引き継ぎできる。録音・送信・自動採点は行わない。
- 第5話到達後に「ドラゴン先生」を解禁。ドラゴンの絵が音と合うか子どもが判定し、間違いなら正しい絵を教える。誤答は3〜5問後に光る支援付きで戻り、通常ゲームと同じ`Attempt`・習熟状態へ記録する。
- 連続日数を要求しない「記憶の星座」を追加。正誤に関係なく冒険へ戻った現地日を最大365日保存し、7日分を地図で星として祝う。旧v1保存は既存試行から自動移行する。
- 6問セッションで選択肢の3つ飛ばしが同じ候補を循環する停止不具合を修正し、有限・重複なしの候補生成と回帰テストを追加。

## 検証済み（v4ローカル）

- `npx tsc --noEmit`: 成功。
- `npm test -- --run`: 8ファイル、145テスト成功。
- `npm run build`: 成功。英語RPG遅延JS 85.21KB、CSS 77.15KB。既存初期JS 588.54KBのサイズ警告のみ。
- `npm run lint`: 終了コード0。英語機能の新規警告0件、既存Mancala/Backgammon警告10件のみ。
- `git diff --check`: 成功（Windows改行予告のみ）。
- ブラウザ通し確認: 6問導入→第1話捕獲→第2話アリーナ移動/タップ→第3話錬金マッチ/語順→第4話脱出→最終ダンジョン4部屋→完了。
- 1440×900: body 1440×900、ダンジョン本体右端1426、操作部右端1311。左右の黒帯・下部見切れなし。
- 390×844: 地図と最終ダンジョンのbody 390×844、横はみ出し・縦スクロールなし。最下部操作は734px、部屋は836pxまでで画面内。
- ブラウザconsole error/warningは通し確認中0件。マイクを使わなくても進行可能。
- 再出題の実画面確認: 第1問を誤答→別の5問へ進む→7問目に同じ項目が「おさらい」として再登場→正解の絵だけが発光。Viteエラー表示なし。
- `npm run verify:english-audio`: 100件すべてで教材ID、英文、ファイル、モデル情報、長さ、音量、SHA-256一致。MP3合計約1.31MB、長さ1.2〜3.55秒。`npm run build`の先頭でも自動実行する。
- 音声見本パネル: 1280×720と390×844で表示・前後移動・再生操作を確認。390px幅のカード右端349pxで横はみ出しなし、Viteエラー表示なし。
- 音声確認記録: 旧v1保存から自動移行し、OK／要再確認が排他的に保存されることを自動検査。390×844で0/100→2/100、状態表示、次項目への自動移動、再読込用localStorage、横はみ出し0、console error 0件を確認。
- ドラゴン先生: 390×844で地図・ゲームともbody幅390px、横はみ出し0。わざと誤答→別の4問→7問目に「おさらい」と光る正解が再登場。1280×720も左右の黒帯・下部見切れなし。両幅で遷移と操作を確認し、console/page errorは0件。
- 記憶の星座: 連続しない3日が`3/7`になること、診断日は除外されること、早朝のISO時刻ではなく端末の現地日で保存されることを自動・ブラウザ双方で確認。
- 完成監査: 100教材の空欄・選択肢重複・前提ID・前提循環、全13話の案内役/精霊参照、1・3・7・14・30日の全復習間隔を自動検査する回帰テストを追加。

## 実装計画との照合

- 第1島の4地域、12メインクエスト、最終4部屋、8精霊と進化、100教材（16/48/20/8/8）、6問導入、共通学習記録、版付き保存、JSON復元、ローカル音声、録音非保存は実装・自動検査済み。
- アリーナと錬金は当初案のCanvasではなく、キーボード・タップ・読み上げ・ターン制を同じ要素で扱えるセマンティックDOM＋純TypeScriptを採用した。ランタイム依存は追加していない。
- 公開判断に必要な人間の全音声確認、家族実機プレイ、ユーザーのプレビュー承認だけは自動検査で代替しない。

## 次にすること

1. 既存4モードを継ぎ足さず、子どもが英語を使う必然性のあるゲームループを白紙から設計する。
2. 画面試作で「聞いて選ぶ」以外の操作・判断・発見が中心になっているか確認する。
3. 家族テストで面白さと定着を確認し、ユーザーの明示承認を得るまで再公開しない。

## 未完成・誤解させてはいけない点

- 初版は第1島100項目。TOEIC 900点台に必要な数千項目、全7 Partの形式演習、120分の持久力訓練は未実装で、現状だけではTOEIC 900点対策アプリとして未完成。
- Kokoro音声100件の生成・正規化・自動検査は完了。公開前に人間が全100件を実際に聞き、発音・間・音量を確認する必要がある。ファイル不在・再生失敗時だけ端末の`SpeechSynthesis`へフォールバックする。
- 最終ダンジョン内の固定4パズルはその場で安全に再挑戦する設計で、通常4ゲームの3〜5問後キューとは別扱い。
- PR #8で一度公開したが、PR #9でサイトから非公開化済み。
- 家族実機プレイと全100音声の人間による通し聴取は未確認で、公開後の継続観察項目。
- 有料API、ランタイムAI、Supabase同期、ログイン、広告、課金、録音保存は初版に入れない。

## 廃止した方向

- どのモードも「音を聞いて同じ選択肢を押す」共通セッション。
- 診断成績で物語や精霊を飛ばす進行。
- 固定3〜4教材を全話で繰り返す実装。
- 最終章で通常モードをもう一度行うだけの構成。
## 2026-08-12 追記: オンライン同ルーム再戦とUNO選択パネル改善

今回の作業:

- UNO
  - `src/features/uno/UnoGamePage.tsx`
    - 色選択、7交換相手選択、カラー ルーレット、UNO自動宣言の中央パネルを専用CSSクラス化。
    - 選択肢の説明文を追加し、色ボタン・相手選択ボタンを大きくして視認性と押しやすさを改善。
  - `src/features/uno/UnoOnlineGamePage.tsx`
    - ゲーム終了後、ホストが「同じルームでもう一度遊ぶ」を押すと同じ `uno_rooms` 行の `game_state` を新しい初期状態に差し替える。
    - 参加者名、CPU設定、CPU強さ、通常版/ハード版を維持して再戦する。
    - ゲスト側には、ルーム主が再戦開始すると自動で切り替わる説明を表示。
- マンカラ
  - `src/features/mancala/MancalaOnlineGamePage.tsx`
    - ゲーム終了後、ホストが同じルームで再戦できるボタンを追加。
    - 再戦時に `gameId` が変わった更新は `turnCount` が0へ戻っても受け取るよう、Realtime/poll受信条件を修正。
    - プレイヤー名、CPU設定、CPU強さ、人数を維持して再戦する。
- バックギャモン
  - `src/features/backgammon/BackgammonOnlineGame.tsx`
    - オンライン終了オーバーレイの「もう一度戦う」を有効化。
    - ホストは同じ `backgammon_rooms` 行の `game_state` を初期化して再戦開始。
    - ゲストが押した場合は、ルーム主が開始する必要がある旨をトースト表示。
- 共通CSS
  - `src/styles/global.css`
    - `.uno-pending-panel` 系のスタイルを追加。
    - 中央パネルの背景、枠、影、説明文、色ボタン、相手選択ボタンを高コントラスト化。
    - モバイル幅でも選択パネルが潰れにくいよう調整。

検証:

- `npx tsc --noEmit`: 成功
- `npx vitest run src/features/uno/unoRules.test.ts src/features/uno/unoOnline.test.ts src/features/mancala/mancalaRules.test.ts src/features/backgammon/backgammonRules.test.ts`: 4 files / 90 tests passed
- `npm run build`: 成功。英語音声100件検証成功。Viteの500kB超chunk警告のみ。
- `npm test -- --run`: 8 files / 145 tests passed
- `npm run lint`: 終了。既存のBackgammon/Mancala警告のみ。
- `git diff --check`: 成功。Windows改行予告のみ。
- in-app browserで `http://127.0.0.1:5175/` を開き、トップページとUNOオンラインルーム画面の表示、console error 0件を確認。

未確認・次に見ること:

- Supabaseを使った実際の2タブ対戦で、各ゲーム終了後にホストが再戦ボタンを押し、ゲスト画面が同じルームのまま新しい盤面へ切り替わるか確認する。
- UNOの色選択/7交換相手選択パネルはコードとビルドで確認済みだが、実プレイ中の発生状態で最終的な見た目をユーザーChrome上でも確認する。
