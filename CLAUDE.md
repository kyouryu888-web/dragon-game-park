# Dragon Game Park

家族向けミニゲーム集サイト。Vite + React + TypeScript、状態同期に Supabase、
ホスティングは Vercel（`main` へのマージで自動的に本番デプロイされる）。

現在のゲーム: マンカラ・UNO・バックギャモン（すべて `src/features/<game>/` 配下）。

## 作業を始める前に

- **今なにをやっている途中か**は `CLAUDE_HANDOFF_CURRENT.md` を読んでください。前回セッションの
  方針・進捗・次にやることがまとまっています。
- **新しいゲームを追加する・既存ゲームの配線を触る**場合は、まず `docs/NEW_GAME_CHECKLIST.md`
  （配線4箇所・推奨構成・確認手順の要約）を読んでください。実装パターンの詳細が必要になったら、
  そこの索引を使って `NEW_GAME_PROMPT.md` の該当箇所だけを読むこと（全文854行を読まない）。
  Claude Code では `/add-game` スキルが同じ手順（確認事項→実装順→検証→公開）を呼び出します。

## PRを作る・マージする前に

**必ず `PUBLISHING.md` を読んで、そこに書かれた手順どおりに進めてください。**
ローカルの型チェックだけで安心せず本番と同じビルドコマンドで確認すること、
マージ前後でVercel/CIのステータスを確認すること、オンライン対戦機能のSupabase
テーブル作成はユーザーに実行してもらう必要があることなど、実際にこのプロジェクトで
起きた失敗から得た手順が書かれています。

Claude Code では `/publish-game` スキルからも同じ手順を呼び出せます。

## 引き継ぎの運用

作業が一段落したら `CLAUDE_HANDOFF_CURRENT.md` を更新してください。大きな節目では
`handoffs/YYYY-MM-DD-*.md` に保存版も作成してください。
