# Dragon Game Park — AI Agent Instructions

家族向けミニゲーム集サイト。Vite + React + TypeScript、状態同期に Supabase、
ホスティングは Vercel（`main` へのマージで自動的に本番デプロイされる）。

現在のゲーム: マンカラ・UNO・バックギャモン（すべて `src/features/<game>/` 配下）。

## 作業を始める前に

- **今なにをやっている途中か**は `CLAUDE_HANDOFF_CURRENT.md` を読んでください。前回の作業内容・
  方針・次にやることがまとまっています。
- **新しいゲームを追加する・既存ゲームの配線を触る**場合は、`CLAUDE_HANDOFF_CURRENT.md` の
  「サイト全体のアーキテクチャ」節にある配線箇所とファイル構成パターンに従ってください。

## PRを作る・マージする前に

**必ず `PUBLISHING.md` を読んで、そこに書かれた手順どおりに進めてください。**
このファイルには、ローカルの型チェックだけでは検出できない本番ビルドエラーの確認方法、
`gh` CLIが使えない環境でのPR作成・マージのAPIレシピ、マージ前後でのデプロイ状態確認、
オンライン対戦機能のSupabaseテーブル作成をユーザーに依頼する手順など、実際にこの
プロジェクトで起きた失敗から得られた具体的な注意点がすべて書かれています。

## 引き継ぎの運用

作業が一段落したら `CLAUDE_HANDOFF_CURRENT.md` を更新してください。
